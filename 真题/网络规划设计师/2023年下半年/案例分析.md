# 2023年下半年网络规划设计师 - 案例分析

> **考试时间**: 2023年11月5日
> **题型**: 案例分析题
> **考试形式**: 笔试
> **数据来源**: 希赛网(educity.cn)考生回忆版 / CSDN博客
> **完整度**: ★★★★☆ (3题，试题一部分答案待补充)

---

## 试题一（25分）

### 【说明】

某企业网络拓扑图如图1所示。

### 【问题1】（16分）

请按照网络安全等级保护第三级要求，为该企业规划安全区域边界层面的网络安全防范方案，简要说明方案中网络安全设备的部署规划(包括设备名称、部署区域、部署方式)，并说明每个设备在网络安全防范体系中的作用。

**参考答案：**

根据等保三级要求，安全区域边界层面需要部署：

1. **防火墙**：部署在网络边界（互联网出口、内外网之间），采用透明/路由模式部署，实现访问控制、包过滤、状态检测等功能。
2. **入侵防御系统(IPS)**：部署在核心交换机与防火墙之间，采用串联模式，实时检测和阻断网络攻击。
3. **Web应用防火墙(WAF)**：部署在Web服务器前端，保护Web应用免受SQL注入、XSS等攻击。
4. **上网行为管理设备**：部署在互联网出口，对内网用户上网行为进行审计和管控。
5. **网络审计系统**：旁路部署，对网络流量进行审计记录。
6. **防病毒网关**：部署在网络边界，检测和拦截恶意流量。

### 【问题2】（6分）

在网络边界部署防火墙时，防火墙会默认划分Local、（1）、Untrust、DMZ四个安全区域，根据业务需要，管理员新建了一个名为Server的安全区域，优先级为90，这五个安全区域按照优先级从高到低的排序为(2)>(3)>(4)>(5)>(6)。

**参考答案：**

(1) Trust

优先级排序：(2) Local > (3) Trust > (4) Server > (5) DMZ > (6) Untrust

> **解析：** 华为防火墙默认安全区域优先级：Local(100) > Trust(85) > DMZ(50) > Untrust(5)。Server自定义优先级为90，介于Local和Trust之间，但根据题意Server优先级90，排序为：Local(100) > Server(90) > Trust(85) > DMZ(50) > Untrust(5)。

**注：根据题目具体描述，正确排序应为：Local(100) > Server(90) > Trust(85) > DMZ(50) > Untrust(5)**

### 【问题3】（3分）

以下为防火墙策略配置代码。

```
[FW]policy interzone trust untrust inbound
FW-policy-interzone-trust-untrust-inbound
```

（此部分题目不完整，来源为考生回忆版，具体配置命令需补充）

---

## 试题二（25分）

### 【说明】

某企业网络拓扑图如图1所示，已知总部Core-1与分部Core-2之间租用专线链路互联，作为两个单位主通信线路，总部Router-1与分部Router-2之间通过Internet线路构建VPN通道，作为两个单位备用通信线路。总部Router-1、Core-1与分部的Router-2、Core-2之间运行OSPF，配置为单区域Area0。NGFW-1、NGFW-2以透明模式（业务接口工作在二层）部署在网络中。

### 【问题1】（10分）

该企业规划的VPN通道起点和终点分别为总部Router-1、分部Router-2的Internet接口，通过VPN通道传输的数据应进行加密保护，同时尽可能的提升总部与分部之间VPN通道数据传输的效率。网络管理员小A经过需求分析，可选的VPN技术有GRE VPN技术和IPSec VPN技术，结合题目请回答以下问题。

1. 小A进行了GRE与IPSec的优劣势分析，发现二者优缺点互补，将二者同时启用才能满足业务需求，请问将二者同时启用的VPN技术是(1)。
2. 小A规划IPSec的封装模式选择(2)，对传输的IP报文进行保护的安全协议选择（3）。
3. 在此场景下，IPSec通过ACL定义需要保护的数据流，参照以下地址规划表，则总部Router-1上配置ACL匹配报文的源地址为(4)，目的地址为(5)。

**参考答案：**

(1) GRE over IPSec（GRE与IPSec结合）

(2) 传输模式（Transport Mode）

> **解析：** 在GRE over IPSec场景中，GRE已经做了封装，IPSec只需对GRE报文进行加密保护，使用传输模式效率更高。

(3) ESP（Encapsulating Security Payload，封装安全载荷）

(4) 总部内网网段地址

(5) 分部内网网段地址

### 【问题2】（8分）

在网络中配置OSPF时，网络管理员进行了优化操作，请回答以下问题:

1. 为提高链路状态变化时OSPF的收敛速度，可在Router-1、Router-2、Core-1、Core-2上配置(6)与OSPF联动，可以快速检测链路状态，使得故障检测时间可以达到毫秒级。
2. 管理员规划在Router-1、Router-2上为OSPF引入缺省路由，由OSPF将缺省路由通告全网。在Router-1、Router-2采用手动配置命令方式，配置完成后路由器将产生一个(7)LSA，并通告到全网，达到全网缺省路由学习的目的。
3. 为提高OSPF的安全性，当用户接口启用OSPF时，可在用户接口上配置(8)，防止非法设备接入用户网络与现网设备建立邻居关系，这也是防止路由环路的一种方法;同时启用OSPF的(9)认证，对本区域所有接口下的OSPF报文进行认证，防止非法设备与网络设备建立邻居关系。

**参考答案：**

(6) BFD（Bidirectional Forwarding Detection，双向转发检测）

(7) Type-5 LSA（AS外部LSA）/ 5类LSA

> **解析：** 通过`default-route-advertise always`命令，OSPF会产生一条Type-5 LSA（AS External LSA），将缺省路由通告到整个OSPF域。

(8) silent-interface（静默接口）

(9) 区域（Area）认证

### 【问题3】（4分）

该企业计划对总部数据中心的WEB业务进行IPv6改造升级。网络管理员对现网评估后规划在Router-1配置NAT64方式实现，通常该方式需要搭配 (10) 一起实现。

已知NAT64前缀为:2001:CD:5:10A::/64，WEB服务器IPv4地址为:124.75.36.100，请问IPv6用户最终向该WEB服务器发起访问的v6地址为(11)。

**参考答案：**

(10) DNS64

(11) 2001:CD:5:10A::7C4B:2464 或 2001:0CD:0005:010A::124.75.36.100

> **解析：** NAT64将IPv4地址嵌入IPv6前缀中。124.75.36.100转换为十六进制：124=0x7C, 75=0x4B, 36=0x24, 100=0x64，因此IPv6地址为2001:CD:5:10A::7C4B:2464。

### 【问题4】（3分）

为保证企业内网安全，该企业计划启用网络准入认证方案，对所有接入网络的设备进行统一的认证和访问授权管理。已知现网设备支持的认证方式有PPPOE、802.1X、MAC认证和WEB认证。请针对以下场景选择合适的认证方式，同时保证不为业务带来额外的开销。

1. 企业内生产部门、研发部门等人员集中、信息安全要求严格的区域，设备接入网络需进行(12)认证。
2. 针对企业会客厅、接待室、访客室等访客、临时人员可能停留的区域，开通访客网络进行 (13)认证。
3. 企业中还存在打印机、传真机、智能门锁等设备，此类设备入网可采用(14)认证。

**参考答案：**

(12) 802.1X

> **解析：** 802.1X认证安全性高，适合对信息安全要求严格的区域。

(13) WEB（Portal）

> **解析：** WEB认证（Portal认证）无需安装客户端，访客通过浏览器即可完成认证，方便临时人员使用。

(14) MAC

> **解析：** 打印机、传真机等哑终端设备不支持安装客户端，MAC认证基于设备MAC地址进行认证，无需人工干预。

---

## 试题三（25分）

### 【说明】

（本题为考生回忆版，题目内容待进一步补充完善）

本题考查的内容涉及数据中心网络规划与设计相关知识。

> **注：** 2023年下半年网络规划设计师案例分析试题三的完整内容暂未从公开渠道获取到完整的考生回忆版。如有后续更新，将补充完善。根据历年考试规律，试题三通常涉及数据中心规划、综合布线、无线网络规划或网络安全等方向。
